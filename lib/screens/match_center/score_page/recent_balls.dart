import 'package:flutter/material.dart';

class RecentBalls extends StatelessWidget {
  const RecentBalls({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
